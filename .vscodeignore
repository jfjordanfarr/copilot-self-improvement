.vscode/**
.vscode-test/**
.vscode-test.mjs
# Exclude source files, as the compiled output in dist/ is used
src/**
# Exclude node_modules from the package
node_modules/
# Exclude build artifacts that are not the final dist output or are specific to dev
*.vsix
*.log
webpack.config.js
.gitignore
CHANGELOG.md
README.md
LICENSE
tsconfig.json
eslint.config.mjs

# If you don't want to ship .map files (optional, but reduces VSIX size):
# **/*.map

# Keep .vscodeignore itself out of the package
.vscodeignore